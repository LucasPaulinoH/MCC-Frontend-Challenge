import SignOutButton from "@/components/Button/SignOutButton";
import { AppContainer } from "@/styles/shared";

const Home = () => {
  return (
    <AppContainer>
      <h1>Home</h1>

      <SignOutButton />
    </AppContainer>
  );
};

export default Home;
